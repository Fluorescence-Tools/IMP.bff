---
type: PRD
prd: "108"
title: "PRD-108: rotamer ensembles for FRET docking — R1 positions in fps.json, the AV↔rotamer table, and Langevin/Brownian dye samplers"
description: cgdye can now be trusted (PRD-107) but still feeds nothing into the FRET docking layer. Give docking a rotamer ensemble object (RotamerEnsemble, an AccessibleVolume with per-rotamer centre + dipole + weight so the full pair matrix R_ij, κ²_ij, w_i·w_j → FRET rate/E distributions is available), a fps.json position type "R1" (FRETpredict-style 1:1 screened library) with a writer for rotamer-derived positions and distances, an authoritative AV↔rotamer cross-validation table on hGBP1 481 and T4L, and real Langevin/Brownian dynamics of an explicit dye with thermodynamic pins. Docking integration stays Python-only; a C++ R1 position inside AVNetworkRestraint is the gated follow-on decided by the table.
status: in-progress
resource: /Users/tpeulen/dev/imp.bff
tags: [prd, imp.bff, cgdye, rotamer, ensemble, fps.json, R1, docking, AVNetworkRestraint, langevin, brownian, validation]
timestamp: '2026-08-17T00:00:00Z'
---

# PRD-108: rotamer ensembles for FRET docking — R1 positions, the AV↔rotamer table, Langevin/Brownian dye samplers

Depends on [PRD-107](prd-107.md). Decisions (user, 2026-08-17 grilling):

1. **Docking integration is Python-only now.** A C++ `R1` position type
   inside `AVNetworkRestraint` (point cloud in the site frame, clash
   re-weighting via the PRD-105 occupancy map) is a follow-on gated by the
   AV↔rotamer table (stage 2).
2. **`simulation_type = "R1"`** = FRETpredict-style 1:1: the full library
   transformed into the site frame and screened against the protein, per
   rotamer **centre + dipole + weight** kept (all atoms retained), so the pair
   matrix R_ij, κ²_ij, w_i·w_j → FRET rate distribution and E_static/E_dyn are
   available downstream. R2/R3 reserved (R2 = sampled library, R3 = mixture).
   C++ warns on unknown types; `fps_positions_for_docking()` filters.
3. Object: **`RotamerEnsemble(fret.av.AccessibleVolume)`** — `points` (N,4) =
   centre + weight so every AV helper works unchanged; pair/rate kernel and κ²
   live in `IMP.bff.fret`.
4. AV↔rotamer differences are **recorded** (table authoritative), pins guard
   drift; not a gate.
5. Samplers: real Langevin, `integrator="md"|"bd"`; the collision walk stays
   `sample-dof-walk`; thermodynamic pins (equilibrium populations vs
   Boltzmann `generate_linker_rotamers`, md ⇄ bd, oscillator temperature,
   free-particle MSD).

## Stages

### Stage 0 — `RotamerEnsemble` + pair kernel in `fret` (done 2026-08-17)
* `fret/distance.py`: `fret_pair_geometry(points1, w1, points2, w2, mu1=None,
  mu2=None)` → R_ij, κ²_ij (2/3 without dipoles), normalised w_i·w_j, ⟨κ²⟩;
  `fret_pair_efficiencies(geometry, forster_radius_A, tau0=None)` → static /
  dynamic1 / dynamic2, per-pair E, rate ratio, k_fret; `fret_pair_distribution`
  = both. `test/fret/test_pair_distribution.py`.
* `cgdye/rotamer/ensemble.py`: `RotamerEnsemble(AccessibleVolume)`
  (`points` = centre + weight; `mu`, `atoms`, `atom_names`, `resnames`,
  `energies`, `partition`, `library`, `chain`, `residue`;
  `params.simulation_type = "R1"`), `RotamerEnsemble.from_site(structure, chain,
  residue, library, *, temperature, electrostatic, potential, ignore_h,
  sigma_scaling, epsilon_scaling, frame_index, position_name)`,
  `.pair_geometry / .pair_distribution / .fret_efficiencies(other,
  forster_radius | donor+acceptor)`, `rotamer_ensembles_from_fps(fps_json,
  structure, library_map)`; module helpers `resolve_backbone_site`,
  `backbone_rotation`, `transform_library_to_site`, `selector_atom_indices`.
  `RotamerFRET._frame_fret` is now built on two ensembles + the fret kernel —
  the FRETpredict pins (stage 3 of PRD-107) pass unchanged (5e-8).
  `test/cgdye/rotamer/test_rotamer_ensemble.py`: AV helpers accept it, Hsp90
  numbers equal FRETpredict's and RotamerFRET's, full 37×7 matrix, isotropic
  κ² against a plain AV, positions from fps.json. Exports:
  `IMP.bff.RotamerEnsemble`, `rotamer_ensembles_from_fps`, `SIMULATION_TYPE_R1`,
  `fret_pair_*`.
### Stage 1 — fps.json `R1` positions + writer (+ C++ warning) (done 2026-08-17)
* `fret/fps_schema.py`: `SIMULATION_TYPES += ("R1",)`; position fields
  `rotamer_library` (required for R1), `dye_name`, `temperature`,
  `electrostatic`, `potential`; `validate_position` errors without a library
  and warns when AV parameters sit on an R1 position; docstring states the
  C++ side never reads `simulation_type`. `data/fps_json_schema.json`
  regenerated (sorted keys).
* `src/AV.cpp::set_av_parameter` warns (`IMP_WARN`) on any non-AV1/AV3/XYZ
  `simulation_type` (would silently score as AV1); `test_AVNetworkRestraint.py::
  R1PositionsTests` puts an R1 position into the T4L score set and asserts the
  warning, and that `fps_positions_for_docking` removes it and silences it.
* `fret/io.py::fps_positions_for_docking(positions, distances)`; 
  `cgdye/rotamer/fps.py`: `rotamer_position_payload`, `rotamer_ensemble_payload`,
  `distances_from_ensembles(ensembles, pairs, R0, distance_type=RDAMean|RDAMeanE|Rmp)`
  from the full pair matrix (⟨R_DA⟩_E through the pair efficiencies with per-pair
  κ²), `write_rotamer_fps(path, positions, distances, merge_into=None,
  validate=True)`. Round-trip test in `test_rotamer_ensemble.py` (write →
  validate → read → ensembles back; merge into an AV file). Exports.
### Stage 2 — AV↔rotamer cross-validation table (hGBP1 481, T4L) (done 2026-08-17)
* `cgdye/rotamer/compare_av.py` (`compare_positions`, `compare_pairs`,
  `ensemble_pair_statistics`, `markdown_table`, `summary_numbers`; exports
  `compare_av_and_rotamer_positions/pairs`), script
  `scripts/compare_av_rotamer.py` = `dye rotamer compare-av` (the `rotamer`
  group is now attached to `dye`; `cli.main` defined — it was called but never
  existed). Writes **`okf/validation/av_vs_rotamer.md`** (authoritative) and
  `test/references/cgdye_av_vs_rotamer_pins.json`;
  `test/cgdye/rotamer/medium_test_av_vs_rotamer.py` pins the numbers exactly
  and asserts loose sanity bounds only (Δmean < 20 Å, Δ⟨R_DA⟩ < 20 Å, no
  weighted rotamer inside the protein).
* **What the table says (2026-08-17, T = 298.15 K, no DH, R0 = 52 Å,
  Alexa488 C1R donors / Alexa594 C1R acceptors, AV positions as authored with
  the default FPS strip):**
  - Position clouds: AV rms extent 11–13 Å vs rotamer 8–12 Å (cutoff10) /
    5–10 Å (cutoff30); Δ mean position median 5–7 Å, max 13–14 Å.
  - Pairs: |Δ⟨R_DA⟩| median 3–4 Å (T4L), 6–12 Å (hGBP1 481 pairs), max ≈ 10–16 Å.
  - **T4L against the 99 experimental distances: Σχ² AV 155, rotamer 257
    (cutoff10) / 141 vs AV 52 on the 66 pairs where the cutoff-30 ensembles are
    valid** — the AV model fits the T4L data clearly better than the screened
    libraries. With cutoff30, 33 of 99 T4L pairs involve a site where every
    rotamer clashes (Z < 0.05, e.g. 22D: FRETpredict's uniform fallback,
    flagged ‡).
  - FRETpredict's soft screening (σ-scaling 0.5) leaves 65–85 % of the
    ensemble weight on rotamers whose heavy atoms come within 2.5 Å of the
    protein (min 1.3–1.6 Å for w > 1e-3) — recorded per position.
* **Gate decision for the C++ `R1` follow-on:** not motivated by this table —
  rotamer ensembles neither reproduce the AVs nor fit the T4L data better;
  the useful follow-on is per-dye *AV-parameter calibration* against the
  ensembles (linker length/width from the rotamer extents) and a harder
  screening (σ-scaling 1) study, both recorded as follow-ons.
### Stage 3 — Langevin / Brownian dye samplers with thermodynamic validation

## Follow-on (gated by stage 2)
C++ `R1` in `AVNetworkRestraint`; per-dye AV-parameter calibration against
the rotamer clouds.

## Implementation record

* 2026-08-17 — Stage 0 landed (see above).
* 2026-08-17 — Stage 1 landed (see above).
* 2026-08-17 — Stage 2 landed (see above): the table is in `okf/validation/av_vs_rotamer.md`.
