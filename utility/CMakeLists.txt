# Autogenerated by tools/build/setup_cmake.py
# from tools/build/cmake_templates/ModuleUtil.cmake
# Do not edit - any changes will be lost!

include_directories()
link_directories()
add_definitions("-DIMP_EXECUTABLE")

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} ${FLUORESCENCE_CXX_FLAGS}")

include(Files.cmake)

foreach (bin ${cppfiles})
   GET_FILENAME_COMPONENT(name ${bin} NAME_WE)
   add_executable(IMP.fluorescence-${name} ${bin})
   target_link_libraries(IMP.fluorescence-${name}     IMP.fluorescence-lib
    
    )
   set_target_properties(IMP.fluorescence-${name} PROPERTIES
                         RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/module_bin/fluorescence"
                         OUTPUT_NAME ${name})
  set_property(TARGET "IMP.fluorescence-${name}" PROPERTY FOLDER "IMP.fluorescence")
   set(executables ${executables} IMP.fluorescence-${name})
endforeach(bin)

set(IMP_fluorescence_UTILS ${executables} CACHE INTERNAL "" FORCE)
